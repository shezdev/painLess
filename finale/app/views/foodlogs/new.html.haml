= form_for Foodlog.new do |f|
  = f.label :date
  = f.date_field :customdate
  = f.label :food
  = f.text_field :food
  = f.submit "Add Food"
- if @foodlogs.any?
  - @foodlogs.each do |f|
    %h2= f.customdate
    %h2= f.food
- else
  %h1 No Food Logged Yet
