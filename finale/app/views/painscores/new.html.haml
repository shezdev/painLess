= form_for Painscore.new do |p|
  = p.label :date
  = p.date_field :customdate
  = p.label :score
  = p.text_field :score
  = p.submit :'Add Pain Score'
- if @painscores.any?
  - @painscores.each do |p|
    %h2= p.customdate
    %h2= p.score
- else
  %h1 No Pain Score
